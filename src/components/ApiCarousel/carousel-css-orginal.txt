

.carousel-section {
  width: 100%;
  max-width: 100%;
  position: relative;
  /* Dette trikset tvinger seksjonen ut av sentrerte containere */
  left: 50%;
  right: 50%;
  margin-left: -50vw;
  margin-right: -50vw;
  
  padding: 100px 0 !important;
  background: transparent;
  overflow: hidden;
}

.carousel.is-dragging * {
  pointer-events: none; /* mens du drar: ikke gå inn på lenker*/
}

.carousel{
  width:100%;
  overflow-x:scroll;
  scroll-snap-type:x mandatory;
  scroll-padding-left: calc((100% - 500px) / 2);
  scroll-padding-right: calc((100% - 500px) / 2);
  cursor: grab;
  user-select: none;
  -webkit-user-select: none;
  /* Viktig for touch: tillat horisontal pan */
  touch-action: pan-x;
  scroll-behavior: smooth;
}

.carousel.is-dragging {
  cursor: grabbing;
}

.carousel:active {
  cursor: grabbing;
}

.carousel::-webkit-scrollbar {
  display: none;
}

.carousel-track{
  align-items: center;
  min-height: 700px;
  display:flex;
  gap:80px;
  align-items:center;
  width:max-content;
  padding:0;              /* <- viktig */
}

.carousel-item {
  scroll-snap-align: center;
  /* Store, tydelige kort */
  flex: 0 0 500px; 
  width: 500px;
  display: flex;
  justify-content: center;
  align-items: center;
  transition: transform 0.2s ease-out;
  will-change: transform;
}

@media (max-width: 768px) {
  .carousel-item {
    flex: 0 0 85vw;
    width: 85vw;
  }

  .carousel-track{
    gap: 30px;
  }

  .carousel{
    scroll-padding-left: calc((100% - 85vw) / 2);
    scroll-padding-right: calc((100% - 85vw) / 2);
  }
}


.api-card {
  background: white;
  color: #0F1A1C;
  padding: 50px;
  border-radius: 40px; 
   overflow: hidden;              /* <- klipper innholdet til radius */
  transform: translateZ(0);      /* <- kan stabilisere rendering ved scale */
  backface-visibility: hidden;
  width: 100%;
  min-height: 320px;
  box-shadow: 0 20px 60px rgba(0,0,0,0.1);
  /* Sikrer at innholdet i kortet er sentrert */
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 20px;
  border: none;
}